// ================================================================
//  /township/create — the "Create a new project" wizard.
//  All behavior lives in the client CreateWizard component.
// ================================================================

import CreateWizard from "../../components/create/CreateWizard";

export default function CreateProjectPage() {
  return <CreateWizard />;
}
